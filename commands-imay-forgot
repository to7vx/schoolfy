#cd "f:\Schoolfy all on one\mobile app 2\admin_dashboard" && flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080

#cd "f:\Schoolfy all on one\mobile app 2\admin_dashboard" && flutter run -d chrome --web-port=8083

#cd "f:\Schoolfy all on one\mobile app 2\pickup_display_web" && flutter run -d chrome --web-port=8081
#flutter run -d chrome --web-port=8081
#flutter run -d chrome --web-port=8083

#admin@schoolfy.com


#http://localhost:8081/

# Admin Dashboard Commands
cd "f:\Schoolfy all on one\mobile app 2\admin_dashboard" && flutter run -d web-server --web-port=8080

# Mobile App Commands  
cd "f:\Schoolfy all on one\mobile app 2\schoolfy_app" && flutter run

# Pickup Display Commands
cd "f:\Schoolfy all on one\mobile app 2\pickup_display_web" && flutter run -d chrome --web-port=8081

# Firebase Commands
firebase login
firebase deploy --only firestore:rules
firebase deploy --only firestore:indexes
firebase deploy --only hosting

# Flutter Commands
flutter clean
flutter pub get
flutter build web

# Git Commands
git status
git add .
git commit -m "message"
git push origin main
