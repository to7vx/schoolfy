{
  "rules": {
    "pickupQueue": {
      ".read": true,
      ".write": true,
      "$date": {
        "$pickupId": {
          ".validate": "newData.hasChildren(['studentId', 'studentName', 'grade', 'time', 'guardianName'])"
        }
      }
    }
  }
}
