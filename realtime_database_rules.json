{
  "rules": {
    "pickupQueue": {
      ".read": true,
      ".write": "auth != null",
      "$date": {
        "$pickupId": {
          ".validate": "newData.hasChildren(['studentId', 'studentName', 'grade', 'time', 'guardianName']) && auth != null"
        }
      }
    }
  }
}
